# 3진법 뒤집기

##### 풀이중

```javascript
function solution(n) {
    let ternary = 0;
    let i = 1;
    while (n > 0){
        const share = Math.pow(i,3);
        const rest = n % (share);
        console.log("rest:", rest, ", share:", share)
        ternary += rest;
        n -=rest;
        i++;
        
    }
    console.log(ternary);
        
    var answer = 0;
    return answer;
}
```

